{
  "name": "sabhya-task-tracker",
  "version": "1.0.0",
  "description": "AI-powered email task tracker using Gmail and Claude",
  "main": "scripts/sync-emails.js",
  "type": "commonjs",
  "scripts": {
    "sync": "node scripts/sync-emails.js",
    "setup": "node scripts/setup-oauth.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.0",
    "googleapis": "^140.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
